                        suffix: ':1',
+                    SizedBox(height: widget.compact ? 8 : 12),
+                    const Text(
+                      'RPM is estimated from smoothed GPS speed + load. Display inertia is added to mimic CVT response. Use OBD-II for actual engine RPM.',
+                      maxLines: 2,
+                      style: TextStyle(
+                        color: Color(0xFF657080),
+                        fontSize: 9.5,
+                        height: 1.25,
+                        fontWeight: FontWeight.w600,
                       ),
                     ),
                   ],
                 ),
-                SizedBox(height: compact ? 8 : 12),
-                const Text(
-                  'RPM is reverse-engineered from GPS speed + acceleration. CVT can run a different real RPM. Use OBD-II for actual engine data.',
-                  maxLines: 2,
-                  style: TextStyle(
-                    color: Color(0xFF657080),
-                    fontSize: 9.5,
-                    height: 1.25,
-                    fontWeight: FontWeight.w600,
-                  ),
-                ),
-              ],
+              ),
+            ],
+          );
+        },
+      ),
+    );
+  }
+}
+
+class _SmoothRollingNumber extends StatelessWidget {
+  const _SmoothRollingNumber({
+    required this.value,
+    required this.width,
+    required this.fontSize,
+    required this.fontWeight,
+    required this.letterSpacing,
+    this.step = 1,
+    this.slotHeight,
+    this.alignment = Alignment.center,
+  });
+
+  final double value;
+  final int step;
+  final double width;
+  final double fontSize;
+  final FontWeight fontWeight;
+  final double letterSpacing;
+  final double? slotHeight;
+  final Alignment alignment;
+
+  @override
+  Widget build(BuildContext context) {
+    final scaled = math.max(0, value / step);
+    final lowerIndex = scaled.floor();
+    final upperIndex = lowerIndex + 1;
+    final fraction = scaled - lowerIndex;
+    final height = slotHeight ?? fontSize * 0.84;
+    final style = TextStyle(
+      color: Colors.white,
+      fontSize: fontSize,
+      height: 0.82,
+      fontWeight: fontWeight,
+      letterSpacing: letterSpacing,
+      fontFeatures: const <ui.FontFeature>[
+        ui.FontFeature.tabularFigures(),
+      ],
+    );
+
+    Widget number(int index) => SizedBox(
+          width: width,
+          height: height,
+          child: Align(
+            alignment: alignment,
+            child: Text(
+              (index * step).toString(),
+              maxLines: 1,
+              style: style,
             ),
           ),
-        ],
+        );
+
+    return SizedBox(
+      width: width,
+      height: height,
+      child: ClipRect(
+        child: Stack(
+          alignment: alignment,
+          children: <Widget>[
+            Transform.translate(
+              offset: Offset(0, -fraction * height),
+              child: Opacity(
+                opacity: (1 - (fraction * 0.24)).clamp(0.0, 1.0),
+                child: number(lowerIndex),
+              ),
+            ),
+            Transform.translate(
+              offset: Offset(0, (1 - fraction) * height),
+              child: Opacity(
+                opacity: (0.76 + (fraction * 0.24)).clamp(0.0, 1.0),
+                child: number(upperIndex),
+              ),
+            ),
+          ],
+        ),
       ),
     );
   }
@@ -804,13 +1024,15 @@
 class _InstrumentMetric extends StatelessWidget {
   const _InstrumentMetric({
     required this.label,
-    required this.value,
     required this.suffix,
+    this.value,
+    this.valueWidget,
     this.warning = false,
-  });
+  }) : assert(value != null || valueWidget != null);
 
   final String label;
-  final String value;
+  final String? value;
+  final Widget? valueWidget;
   final String suffix;
   final bool warning;
 
@@ -834,16 +1056,20 @@
           Row(
             crossAxisAlignment: CrossAxisAlignment.end,
             children: <Widget>[
-              Text(
-                value,
-                style: const TextStyle(
-                  color: Colors.white,
-                  fontSize: 28,
-                  height: 1,
-                  fontWeight: FontWeight.w500,
-                  letterSpacing: -1.1,
-                ),
-              ),
+              valueWidget ??
+                  Text(
+                    value!,
+                    style: const TextStyle(
+                      color: Colors.white,
+                      fontSize: 28,
+                      height: 1,
+                      fontWeight: FontWeight.w500,
+                      letterSpacing: -1.1,
+                      fontFeatures: <ui.FontFeature>[
+                        ui.FontFeature.tabularFigures(),
+                      ],
+                    ),
+                  ),
               const SizedBox(width: 5),
               Padding(
                 padding: const EdgeInsets.only(bottom: 2),
@@ -1229,6 +1455,8 @@
 
   @override
   void paint(Canvas canvas, Size size) {
+    final safeSpeedFraction = speedFraction.clamp(0.0, 1.0);
+    final safeRpmFraction = rpmFraction.clamp(0.0, 1.0);
     final center = Offset(size.width * 0.5, size.height * 0.49);
     final radius = math.min(size.width, size.height) * 0.37;
     final rect = Rect.fromCircle(center: center, radius: radius);
@@ -1242,20 +1470,99 @@
       ..color = const Color(0xFF1C2530);
     canvas.drawArc(rect, start, sweep, false, base);
 
+    // 0-240 km/h reference ticks. The digital value remains dominant, but
+    // these ticks make the movement read like an instrument rather than a
+    // progress ring.
+    for (var i = 0; i <= 24; i++) {
+      final fraction = i / 24;
+      final angle = start + (sweep * fraction);
+      final major = i.isEven;
+      final outerRadius = radius - 9;
+      final innerRadius = outerRadius - (major ? 11 : 6);
+      final active = fraction <= safeSpeedFraction + 0.001;
+      final tickPaint = Paint()
+        ..style = PaintingStyle.stroke
+        ..strokeCap = StrokeCap.round
+        ..strokeWidth = major ? 2.2 : 1.2
+        ..color = active
+            ? const Color(0xFFCAD3DC)
+            : const Color(0xFF303A46);
+      final outer = Offset(
+        center.dx + math.cos(angle) * outerRadius,
+        center.dy + math.sin(angle) * outerRadius,
+      );
+      final inner = Offset(
+        center.dx + math.cos(angle) * innerRadius,
+        ce