--- a/lib/features/head_unit/drive_deck_panel.dart
+++ b/lib/features/head_unit/drive_deck_panel.dart
@@ -1160,15 +1160,15 @@
         children: <Widget>[
           Text(
             label,
-            style: const TextStyle(
+            style: TextStyle(
               color: Colors.white,
               fontSize: compact ? 14 : 16,
               fontWeight: FontWeight.w700,
             ),
           ),
           const SizedBox(height: 4),
-          const Icon(Icons.navigation_rounded,
-              color: Color(0xFF39BFF0), size: compact ? 16 : 18),
+          Icon(Icons.navigation_rounded,
+              color: const Color(0xFF39BFF0), size: compact ? 16 : 18),
         ],
       ),
     );
