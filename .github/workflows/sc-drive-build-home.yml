name: SC Drive Legacy Home Bridge (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow retired
        run: |
          echo 'This v1.4 Home Bridge pipeline is retired.'
          echo 'Use .github/workflows/sc-drive-apk.yml for SC Drive v1.5.4 and later.'
